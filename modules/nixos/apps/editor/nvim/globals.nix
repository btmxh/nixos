# Neovim global settings and colorscheme
{
  # Global variables
  globals = {
    mapleader = " "; # Set leader key to space
    copilot_nes_debounce = 500; # Copilot suggestion debounce in ms
  };

  # Color scheme configuration
  colorschemes.catppuccin.enable = true;
}
